/*
// /src/store/features/auth/index.ts
import { combineReducers } from '@reduxjs/toolkit';

import personalReducer from './personal/slice.ts';
import addressReducer from './address/slice.ts';
import clientsReducer from './clients/slice.ts';
import boardReducer from './board/slice.ts';
import toastReducer from './toast/slice.ts';

const privateReducer = combineReducers({
    personal: personalReducer,
    clients: clientsReducer,
    address: addressReducer,
    board: boardReducer,
    toast: toastReducer,
});

export default privateReducer;
*/
